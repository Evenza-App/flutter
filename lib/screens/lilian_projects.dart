import 'package:flutter/material.dart';

class LilianProjects extends StatelessWidget {
  const LilianProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}