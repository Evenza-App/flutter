import 'package:flutter/material.dart';

class SaherProjects extends StatelessWidget {
  const SaherProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}