import 'package:flutter/material.dart';

class SaeedProjects extends StatelessWidget {
  const SaeedProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}