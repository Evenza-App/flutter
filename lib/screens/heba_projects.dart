import 'package:flutter/material.dart';

class HebaProjects extends StatelessWidget {
  const HebaProjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}